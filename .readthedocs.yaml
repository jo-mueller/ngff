version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.10"
    nodejs: "20"

submodules:
  include: all
  recursive: true

sphinx:
  configuration: conf.py

python:
  install:
    - requirements: requirements.txt
    - method: pip
      path: ./0.6.dev2
